hl.config({
	input = {
		kb_layout = "us,dk",
		repeat_rate = 60,
		repeat_delay = 200,

		follow_mouse = 2,
	},
})

hl.config({
	cursor = {
		invisible = false,
		no_warps = false,
		hide_on_key_press = true,
	},
})
